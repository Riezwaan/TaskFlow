# Render supplies DATABASE_URL. Without it, local development uses persistent PGlite.
PORT=3000
NODE_ENV=development
# DATABASE_URL=postgresql://user:password@localhost:5432/taskflow
# Use only for a database provider that requires TLS:
# DATABASE_SSL=true
